<UserControl x:Class="Sponge.View.Common.ProcBinControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Sponge.View.Common"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel> 
            <local:UpdateControl/>
            <TextBlock Margin="0,5,0,0">Beta</TextBlock>
            <TextBox Text="{Binding Beta}"/>
            <TextBlock Margin="0,5,0,0">Energy</TextBlock>
            <TextBlock Text="{Binding Energy}"/>
        </StackPanel>
        
        <local:GraphLatticeControl x:Name="GraphLatticeControl" 
                DataContext="{Binding GraphLatticeVm}"
                Grid.Column="1"/>


    </Grid>
</UserControl>
